import 'package:flutter/material.dart';

class FeedbackConstants {
  static Duration openingAnimDuration = const Duration(milliseconds: 250);
  static EdgeInsets miniAppPadding = const EdgeInsets.symmetric(horizontal: 35);
  static BorderRadius miniAppBorderRadius = BorderRadius.circular(12.0);
}

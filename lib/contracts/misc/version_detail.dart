class VersionDetail {
  String packageName;
  String version;
  String buildNumber;

  VersionDetail({
    required this.packageName,
    required this.version,
    required this.buildNumber,
  });
}
